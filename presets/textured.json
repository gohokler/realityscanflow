{
    "name": "textured",
    "description": "Full pipeline: align + mesh + unwrap + texture + export OBJ. Production ready output.",
    "requires": ["input_folder", "output_project", "output_report"],
    "steps": [
        "-set appQuitOnError=true",
        "-addFolder {input_folder}",
        "-align",
        "-selectMaximalComponent",
        "-setReconstructionRegionAuto",
        "-calculateNormalModel",
        "-selectMarginalTriangles",
        "-removeSelectedTriangles",
        "-renameSelectedModel textured_model",
        "-unwrap",
        "-calculateTexture",
        "-exportModel textured_model {output_project}.obj",
        "-save {output_project}",
        "-exportReport {output_report} {overview_template}",
        "-quit"
    ]
}
